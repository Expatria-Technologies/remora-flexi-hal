#probe_basic spindle load indicator

	net spindle-load vfdmod.parameters.vfd-output-current => qtpyvcp.spindle-load-indicator.in-f
    
    #set this to max current of spindle. Will be shown as reported current/max-val
	setp qtpyvcp.spindle-load-indicator.max-val 10
